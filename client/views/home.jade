h1 Welcome to Hit the breaks!

//- If not logged in
button(ng-if="!isUser" ng-click="authenticate('facebook')") Login via Facebook to start reporting incidents

//- If logged in
button(ng-if="isUser" ui-sref="report") Report

button(ui-sref="map") View Map
